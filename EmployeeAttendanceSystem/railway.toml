[build]
command = "dotnet publish -c Release -o ./publish"
builder = "nixpacks"

[deploy]
startCommand = "dotnet ./publish/EmployeeAttendanceSystem.dll --urls http://0.0.0.0:$PORT"
port = 8080

[variables]
PORT = "8080"
ASPNETCORE_ENVIRONMENT = "Production"